name: my-first-flow
on:
  push:
    branches: [main]
    workflow-dispatch:
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world!"
      - uses: actions/checkout@v2
      - run: ls
      - run: echo "Goodbye!"
      - with:
          secret: ${{ secrets.DOCKERHUB_USERNAME }}
      - env:
          secret: ${{ secrets.DOCKERHUB_USERNAME }}