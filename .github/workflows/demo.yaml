on:
  push:
    branches: [main]
  workflow-dispatch:
jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world!"
      - uses: actions/checkout@v2
      - run: ls
      - run: echo "Goodbye!"